node_modules/
.expo/
dist/
web-build/

# Native Projekte werden aus app.json generiert (Prebuild/CNG) und nicht
# eingecheckt. Liegen sie im Repo, ignoriert der Build die Konfiguration aus
# app.json — Icon, Splash, Plugins und Version liefen dann auseinander.
/android/
/ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*
.env
.env.local
.env.*.local
.maestro/.env
supabase.exe
supabase_cli*
supabase/.temp/

# Lokal generierter Wissensgraph (graphify) — Ausgabeartefakt, kein Quellcode.
graphify-out/
.DS_Store
Thumbs.db

# Lokal generierter Wissensgraph (graphify) — Ausgabeartefakt, kein Quellcode.
graphify-out/
